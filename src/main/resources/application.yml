server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://172.17.0.1:3306/patakparlat
    username: root
    password: macilaci2024
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: false
    show-sql: true
logging:
  level:
    org.springframework.web.servlet.handler.AbstractHandlerMapping: DEBUG
